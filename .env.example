# Postgres connection (Supabase: use the "Direct connection" / pooler URL from project settings)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/plfantasy

# Frontend -> API base URL (frontend/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:8000
